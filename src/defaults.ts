/** Maximum search steps per grid calculation; exhaustion is not proof of no space. */
export const DEFAULT_SEARCH_BUDGET = 10_000;
/** Long press duration in milliseconds. */
export const DEFAULT_ACTIVATION_DELAY_MS = 250;
